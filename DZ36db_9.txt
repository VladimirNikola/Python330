1.Выведите список студентов физико-математического факультета.
1.Выведите список студентов физ
SELECT FIO
FROM Student
WHERE [Group] IN
    (SELECT [Group]
    FROM G
    WHERE Kafedra IN
      (SELECT Kafedra
      FROM Kafedra
      WHERE Decanat = 'Физико-математический'
      )
     )

2.Студенты какой кафедры и факультета сдавали социологию.

3.Выведите кафедры, список студентов, список студентов в алфавитном порядке.
SELECT s.FIO, 
    (SELECT g.Kafedra
    FROM G g
    WHERE s."Group"=g."Group") AS [Кафедра]
FROM Student s
ORDER BY s.FIO

4.Вывести номера зачеток только студентов физико-технического факультета.
SELECT N_Z
FROM Zachetki
WHERE Id_Studenta =  
    (
    SELECT ID_st
    FROM Student
    WHERE [Group] IN
        (
        SELECT [Group]
        FROM G
        WHERE Kafedra IN
            (
            SELECT Kafedra
            FROM Kafedra
            WHERE Decanat is 'Физико-технический'
            )
        )
    );    

5.Выведите студентов физико-технического факультета сдавших иностранный язык на 5.


6.Подсчитать сколько различных предметов сдавалось в сессию.
SELECT COUNT(DISTINCT Predmet) AS [Количество предметов]
FROM Exzamen

7.Напишите запрос, который выводит средний балл по экзаменам Васильевой.
SELECT AVG(Ball)
FROM Exzamen
WHERE Id_St = 
    (
    SELECT ID_St
    FROM Student
    WHERE FIO LIKE 'Васильева%'
    )

8.Определите сколько человек учится на каждой специальности.
SELECT COUNT(ID_St) AS [Количество человек], Special AS [Специальность]
FROM Student 
GROUP BY Special

9.Напишите запрос, который покажет все группы физико-технического факультета.
SELECT [Group]
FROM G
WHERE Kafedra IN
        (
        SELECT Kafedra
        FROM Kafedra
        WHERE Decanat IS 'Физико-технический'
        )
10.Вывести список фамилий студентов получивших 5 баллов по дифференциальным уравнениям.
SELECT FIO
FROM Student
WHERE ID_St =
    (SELECT Id_St
    FROM Exzamen
    WHERE Predmet LIKE 'Дифф%' AND Ball = 5
    )

11.Напишите запрос, который покажет,  сколько экзаменов  сдавал Шутов.
SELECT Predmet 
FROM Exzamen
WHERE Id_St =
    (SELECT ID_St
    FROM Student
    WHERE FIO LIKE 'Шутов%'
    )

12.Выведите название кафедры студентов, не сдавших хотя бы один экзамен.
SELECT s.FIO AS [Студент не сдавший экзамен], g.Kafedra AS [Кафедра], e.Predmet AS [Предмет]
FROM G g, Student s, Exzamen e
WHERE g.[Group] = s.[Group] AND 
s.ID_St = e.Id_St AND e.Ball IS NULL

13.Подсчитать количество кафедр на каждом факультете.
SELECT COUNT(Decanat) AS [Количество кафедр], Decanat AS [Факультет]
FROM Kafedra 
GROUP BY Decanat

14.Подсчитать количество пятерок на физико-математическом факультете.
SELECT Predmet, Ball
FROM Exzamen
WHERE Ball = 5 AND Id_St IN
    (SELECT ID_St
    FROM Student
    WHERE [Group] IN
        (SELECT [Group]
        FROM G
        WHERE Kafedra IN
            (SELECT Kafedra
            FROM Kafedra
            WHERE Decanat = 'Физико-математический'
            )
        )
     )

15.Определите номера зачетных книжек группы ФТ151
SELECT g.[Group], z.N_Z
FROM G g, Zachetki z 
WHERE g.[Group] = 'ФТ151'

16.Выведите перечень специальностей физико-математического факультета.
SELECT Predmet
FROM Exzamen
WHERE Id_St IN
    (SELECT ID_St
    FROM Student
    WHERE [Group] IN
        (SELECT [Group]
        FROM G
        WHERE Kafedra IN
            (SELECT Kafedra
            FROM Kafedra
            WHERE Decanat = 'Физико-математический'
            )
        )
     )